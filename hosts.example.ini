[remnawave_node]
example-node ansible_host=127.0.0.1 ansible_user=root ansible_connection=local

[openwrt]
example-router ansible_host=127.0.0.1 ansible_user=root ansible_connection=local

[all:vars]
# For local syntax/lint checks only.
# Real deploy flow uses runtime inventory rendered from GitHub Secrets.
